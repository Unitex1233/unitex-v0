{
  "name": "unitex-monorepo",
  "version": "1.0.0",
  "description": "UniteX Platform Monorepo Orchestrator",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "media-service",
    "blockchain"
  ],
  "scripts": {
    "dev": "concurrently -k -s first -n \"CLIENT,SERVER,MEDIA,CHAIN\" -c \"blue,green,magenta,yellow\" \"npm run dev -w client\" \"npm run dev -w server\" \"npm run dev -w media-service\" \"npm run node -w blockchain\"",
    "install-all": "npm install && npm install --prefix client && npm install --prefix server && npm install --prefix media-service && npm install --prefix blockchain"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
